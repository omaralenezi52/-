# التبعيات الأساسية (أحدث الإصدارات المستقرة)
fastapi>=0.115
uvicorn[standard]>=0.32
pydantic>=2.9
httpx>=0.27
python-dotenv>=1.0

# التحليل الفني — حساب المؤشرات بتحكم كامل (تطابق Backtest/Live)
numpy>=1.26
pandas>=2.2

# الذكاء — عقدة المحلل عبر Claude
anthropic>=1.0

# التنفيذ عبر Interactive Brokers (فعّله عند ربط حسابك + TWS/Gateway)
ib_insync>=0.9.86

# الاختبارات
pytest>=8.3
