# التبعيات الأساسية (أحدث الإصدارات المستقرة)
fastapi>=0.115
uvicorn[standard]>=0.32
pydantic>=2.9
httpx>=0.27
python-dotenv>=1.0

# التحليل الفني (تُضاف عند ربط حساب المؤشرات محلياً)
# pandas>=2.2
# pandas-ta>=0.3.14b0

# التنفيذ عبر Interactive Brokers (اختياري حتى التفعيل)
# ib_insync>=0.9.86

# الذكاء (LangGraph + Claude) — يُضاف في مرحلة التحليل
# langgraph>=0.2
# langchain-anthropic>=0.3

# الاختبارات
pytest>=8.3
